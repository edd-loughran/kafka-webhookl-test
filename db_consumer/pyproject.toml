[project]
name = "db_consumer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "kafka-python",
    "sqlalchemy",
    "psycopg[binary]"
]

[tool.setuptools.packages.find]
include = ["models", "db_repositories"]
